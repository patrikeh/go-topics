package words

import (
	"testing"
)

func Test_LDA(t *testing.T) {
	/* 	lda := NewSimpleLDA(&Configuration{})
	   	corpus := NewCorpus()
	   	_, err := lda.Train(corpus, 100, 1, 0, 0)
	   	assert.Nil(t, err) */
}
